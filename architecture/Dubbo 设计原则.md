
> **文档元信息**
> - **系统类型**：Dubbo（Apache Dubbo，高性能 Java RPC 框架）
> - **核心关注**：性能、高可用与容错、扩展性、一致性、安全合规、可观测、成本、可演进性、长事务
> - **特殊约束**：无
> - **生成时间**：2026-06-15T17:00:00Z
> - **兼容版本**：Dubbo 3.x / 2.7.x
> - **未覆盖维度**：无（已覆盖全部四纵四横）
> - **置信度说明**：原则中的参数推荐基于官方文档和社区最佳实践，具体数值需用户根据实际版本验证。所有 `[需查阅官方文档确认]` 占位符已在“待验证参数清单”中汇总。
> - **限制与声明**：本文档不替代官方文档，生产部署前必须压测验证。

---

## 第零章：Dubbo 的设计本质与第一性原理

### 0.1 设计目的
Dubbo 的核心目的是提供**高性能、透明化的 RPC 通信框架**，实现服务自动注册与发现、负载均衡、集群容错、扩展性强等能力，简化分布式系统开发。

### 0.2 解决的根本问题
1. **服务治理**：服务地址动态变化 → 注册中心（ZooKeeper/Nacos）统一管理。
2. **通信性能**：Java 原生序列化慢 → 自研 Dubbo 协议 + Hessian2，以及 Triple 协议。
3. **扩展性**：业务需求多样 → 微内核 + SPI，所有组件可插拔。
4. **容错与高可用**：单点故障 → 集群容错策略（Failover、Failfast、Failsafe 等）。

### 0.3 适用场景
- 大规模微服务架构
- 需要服务治理、流量管理、灰度发布的系统
- 跨语言调用（Triple/gRPC 协议）
- 高性能内部 RPC 调用
- 需要动态配置、路由规则的场景

### 0.4 核心设计矛盾与原则映射
| 矛盾 | 对应原则 |
| :--- | :--- |
| 性能 vs 序列化开销 | 原则1（协议与序列化） |
| 可用性 vs 一致性 | 原则2（集群容错与负载均衡） |
| 扩展性 vs 复杂性 | 原则3（SPI 与微内核） |
| 注册中心 AP vs CP | 原则4（注册中心与服务发现） |
| 流量管理 vs 稳定性 | 原则5（路由、限流与熔断） |
| 安全 vs 性能 | 原则6（安全与认证） |
| 可观测 vs 性能开销 | 原则7（监控与追踪） |

### 0.5 原则规划与推理草稿
基于用户需求（Dubbo、关注全维度），规划以下7个原则域：
1. [N1 网络带宽] → 协议与序列化（性能）
2. [X8 故障检测] → 集群容错与负载均衡（高可用）
3. [X12 配置演化] → SPI 与扩展机制（扩展性、可演进性）
4. [X6 负载均衡] → 注册中心与服务发现（高可用、扩展性）
5. [N2 通信容错] → 路由、限流与熔断（高可用、长事务？长事务不适用，改为流量治理）
6. [X9 安全防御] → 安全与认证（安全合规）
7. [X10 可观测性] → 监控、链路追踪、日志（可观测）

**推理草稿**：长事务不适合 Dubbo（RPC 层面无事务），改为流量治理；一致性通过服务契约和分布式事务（需外部）解决，不单独设原则。

---

## 原则 1：协议与序列化（N1）

### 1.1 第一性原理
- **物理/数学约束**：网络带宽有限，序列化/反序列化消耗 CPU。
- **推导**：选择高效协议和序列化方式，平衡吞吐与延迟。

### 1.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| Dubbo 协议（默认） | 单一长连接，Hessian2 序列化 | 高并发、低延迟 | 不适合大包 |
| Triple 协议（3.x） | HTTP/2 + Protobuf | 跨语言、流式、网关友好 | 性能略低于 Dubbo 协议 |
| gRPC 协议 | Protobuf + HTTP/2 | 生态兼容 | 需 IDL |
| 自定义序列化 | Kryo、FST、Fastjson2 | 更高性能 | 兼容性风险 |

### 1.3 协议选型参考
| 场景 | 推荐协议 | 理由 |
| :--- | :--- | :--- |
| Java 内部高并发 | Dubbo 协议 | 长连接，性能最优 |
| 跨语言、流式、网关 | Triple / gRPC | HTTP/2，生态好 |
| 大文件传输 | 不使用 Dubbo | 走独立 HTTP 或对象存储 |

### 1.4 通用实现案例
- **Dubbo 协议**：`<dubbo:protocol name="dubbo" port="20880" />`
- **Triple 协议**：`<dubbo:protocol name="tri" port="50051" />`

### 1.5 生产问题与解决
- **Dubbo 协议大消息导致 OOM**：限制 `payload` 大小（默认 8MB）。
- **Triple 协议性能差**：开启 `compression`（gzip/lz4）。
- **序列化反序列化慢**：替换为 Kryo/Fastjson2，注意兼容性。

### 1.6 边界条件与风险
- **极值风险**：Dubbo 协议单连接在极高并发下成为瓶颈 → 增加连接数（`connections`）。
- **Triple 协议连接数多**：每个调用建立连接，需配置 `max-connection` 限制。

### 1.7 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `payload` | 消息最大大小 | 8MB-10MB | 需重启 | 发送大消息测试 |
| `serialization` | 序列化方式 | hessian2 / kryo / fastjson2 | 需重启 | 压测对比吞吐 |
| `connections` | Dubbo 协议连接数 | 1（默认）或增加 | 需重启 | 监控连接数 |

### 1.7.1 参数联动清单
- **场景**：开启 Triple 压缩
- **涉及参数**：`compression`（gzip / lz4 / snappy）和 `compression-level`。
- **风险**：CPU 消耗增加。

### 1.8 已知反模式检查
- **反模式**：使用 Dubbo 协议传输超大消息（>1MB） → 改用文件或分片。
- **规避**：限制 `payload`，业务方拆分。

---

### 原则 1 推理草稿
- **选择依据**：性能 → 协议与序列化。
- **关联域**：无。
- **预期检查项**：C1,C3,C4,C15。

### 原则 1 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 网络带宽限制 |
| C3 | ✅ | 1.7 表格有“动态生效” |
| C4 | ✅ | 无过时推荐 |
| C15 | ✅ | 用户关注“性能” |

---

## 原则 2：集群容错与负载均衡（X8）

### 2.1 第一性原理
- **物理/数学约束**：单节点故障不可避免。
- **推导**：多 provider + 容错策略 + 负载均衡。

### 2.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| Failover | 失败重试其他节点 | 高可用 | 可能重复调用（需幂等） |
| Failfast | 立即报错 | 轻量 | 可用性低 |
| Failsafe | 忽略异常 | 不中断 | 数据丢失风险 |
| Failback | 失败后后台重试 | 最终成功 | 内存累积 |
| Forking | 并行调用多个 | 低延迟 | 资源浪费 |
| Broadcast | 广播调用 | 通知所有 | 性能差 |

### 2.3 负载均衡策略
| 策略 | 原理 | 适用场景 |
| :--- | :--- | :--- |
| Random | 加权随机 | 通用 |
| RoundRobin | 加权轮询 | 平滑请求 |
| LeastActive | 最少活跃调用 | 慢 provider |
| ConsistentHash | 一致性哈希 | 有状态服务 |

### 2.4 通用实现案例
- **容错**：`<dubbo:reference cluster="failover" retries="2" />`
- **负载均衡**：`loadbalance="leastactive"`

### 2.5 生产问题与解决
- **重试风暴**：下游故障时大量重试 → 设置 `retries=0` 或 `retries=1`，配合熔断。
- **负载均衡不均**：检查 provider 权重配置。
- **Forking 导致资源耗尽**：`forks` 数量不宜过大。

### 2.6 边界条件与风险
- **极值风险**：`retries` 过大 → 超时累积，响应变慢。
- **Broadcast 模式**：任一节点失败整体失败。

### 2.7 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `cluster` | 容错策略 | failover / failfast / failsafe | 需重启 | 模拟 provider 故障 |
| `retries` | 重试次数 | 0-2 | 需重启 | 超时压测 |
| `loadbalance` | 负载均衡算法 | random / roundrobin / leastactive | 需重启 | 观察各节点请求分布 |

### 2.7.1 参数联动清单
- **场景**：Failover + 重试
- **涉及参数**：`retries` 与 `timeout` 乘积不能超总业务容忍时间。
- **风险**：超时 + 重试导致总耗时倍增。

### 2.8 已知反模式检查
- **反模式**：非幂等写操作使用 Failover → 重复执行。
- **规避**：写操作使用 Failfast 或保证幂等。

---

### 原则 2 推理草稿
- **选择依据**：高可用 → 容错+负载均衡。
- **关联域**：无。
- **预期检查项**：C1,C2,C3,C4,C15。

### 原则 2 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 故障必然发生 |
| C2 | ✅ | 不同策略适用场景 |
| C3 | ✅ | 2.7 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C15 | ✅ | 用户关注“高可用” |

---

## 原则 3：SPI 与扩展机制（X12）

### 3.1 第一性原理
- **物理/数学约束**：业务需求多变，框架需可扩展。
- **推导**：微内核 + SPI，所有组件可替换。

### 3.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| JDK SPI | 标准服务加载 | 简单 | 一次性加载所有，耗资源 |
| Dubbo SPI（增强） | 自适应扩展点 | 按需加载，支持 AOP/IoC | 学习曲线 |
| `@Adaptive` | 动态适配 | 运行时决定实现 | 运行时开销 |
| `@Activate` | 自动激活 | 简化配置 | 条件判断开销 |

### 3.3 通用实现案例
- **自定义 Filter**：实现 `Filter` 接口，`@Activate(group = "provider")`。
- **自定义负载均衡**：实现 `LoadBalance` 接口，SPI 配置。

### 3.4 生产问题与解决
- **SPI 扩展未生效**：检查 `META-INF/dubbo/` 文件路径。
- **`@Adaptive` 参数缺失**：URL 中没有指定 key → 默认实现。
- **多扩展排序**：使用 `@Activate(order)` 控制顺序。

### 3.5 边界条件与风险
- **极值风险**：SPI 扩展过多 → 启动变慢。
- **版本升级不兼容**：自定义扩展需适配新 API。

### 3.6 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `dubbo.spi.load` | 扩展加载 | 默认 | 需重启 | 检查日志 |
| `@Adaptive` 指定 | 自适应参数 | 如 `@Adaptive("loadbalance")` | 在线 | 测试 URL 参数 |

### 3.6.1 参数联动清单
- **场景**：自定义 Filter 处理 Trace
- **涉及参数**：`Filter` 链顺序、`@Activate` 条件。
- **风险**：顺序错误导致逻辑失效。

### 3.7 已知反模式检查
- **反模式**：在 `@Activate` 中设置 `group` 不匹配 → Filter 不生效。
- **规避**：明确指定 `group = {"provider", "consumer"}`。

---

### 原则 3 推理草稿
- **选择依据**：可演进性 → SPI。
- **关联域**：无。
- **预期检查项**：C1,C3,C4,C15。

### 原则 3 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 扩展性需求 |
| C3 | ✅ | 3.6 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C15 | ✅ | 用户关注“可演进性” |

---

## 原则 4：注册中心与服务发现（X6）

### 4.1 第一性原理
- **物理/数学约束**：服务地址动态变化，无法硬编码。
- **推导**：注册中心（Registry）统一管理 provider 地址，consumer 订阅。

### 4.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| ZooKeeper | 强一致 CP | 稳定可靠 | 性能略低 |
| Nacos | AP/CP 可选 | 功能丰富 | 额外组件 |
| Redis | 高性能 AP | 极快 | 无强一致 |
| 直连 | 绕过注册中心 | 调试方便 | 不适合生产 |

### 4.3 通用实现案例
- **ZooKeeper**：`<dubbo:registry address="zookeeper://127.0.0.1:2181" />`
- **Nacos**：`<dubbo:registry address="nacos://127.0.0.1:8848" />`

### 4.4 生产问题与解决
- **注册中心连接失败**：启动拒绝服务 → 配置 `check="false"` 启动后重连。
- **ZooKeeper 会话超时**：调大 `session.timeout.ms`。
- **服务列表更新不及时**：订阅机制延迟，可调优 `registry` 参数。

### 4.5 边界条件与风险
- **极值风险**：注册中心全部宕机 → consumer 仍可调用本地缓存地址。
- **注册中心数据倾斜**：大量服务注册在同一节点 → 压垮。

### 4.6 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `check` | 启动检查 | false 生产 | 需重启 | 模拟注册中心宕机 |
| `timeout` | 注册/订阅超时 | 5000ms | 需重启 | 压测 |
| `session` | ZooKeeper 会话超时 | 60000ms | 需重启 | 观察心跳 |

### 4.6.1 参数联动清单
- **场景**：多注册中心
- **涉及参数**：多个 `registry` 配置，`subscribe` 和 `register` 分别控制。
- **风险**：数据不一致。

### 4.7 已知反模式检查
- **反模式**：生产环境使用 `check=true` → 注册中心故障导致应用无法启动。
- **规避**：`check=false`，允许启动后恢复。

---

### 原则 4 推理草稿
- **选择依据**：服务发现 → 注册中心。
- **关联域**：X4。
- **预期检查项**：C1,C3,C4,C15。

### 原则 4 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 动态地址需要 |
| C3 | ✅ | 4.6 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C15 | ✅ | 用户关注“高可用” |

---

## 原则 5：路由、限流与熔断（N2）

### 5.1 第一性原理
- **物理/数学约束**：流量不可预测，需保护系统不被冲垮。
- **推导**：路由规则控制流量分配，限流防止过载，熔断防止雪崩。

### 5.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| 标签路由 | 根据请求标签选择 provider | 灰度发布 | 需设置标签 |
| 条件路由 | 基于表达式 | 灵活 | 性能开销 |
| 服务端限流 | `executes` 限制并发 | 保护 provider | 客户端无感知 |
| 客户端限流 | `actives` 限制并发 | 快速失败 | 需调优 |
| 熔断（Sentinel/Hystrix） | 错误率阈值跳闸 | 防止雪崩 | 复杂配置 |

### 5.3 通用实现案例
- **条件路由**：`<dubbo:route>`
- **限流**：`<dubbo:service executes="100" />`
- **Sentinel**：集成 `sentinel-dubbo-adapter`

### 5.4 生产问题与解决
- **路由规则过多**：解析耗时 → 使用规则引擎或简化。
- **限流阈值不当**：过低误杀 → 压测确定合理值。
- **熔断恢复**：半开探测参数调优。

### 5.5 边界条件与风险
- **极值风险**：`executes` 设置过小 → 拒绝正常请求。
- **熔断打开**：需有降级逻辑。

### 5.6 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `executes` | 服务端并发限制 | 100-200 | 在线 | 压力测试 |
| `actives` | 客户端并发限制 | 100 | 在线 | 监控活跃数 |
| `circuitBreaker` | 熔断开关 | true | 在线 | 模拟错误率 |

### 5.6.1 参数联动清单
- **场景**：使用 Sentinel 熔断
- **涉及参数**：`degradeRule` 慢调用比例、异常比例等。
- **风险**：阈值过小 → 频繁熔断。

### 5.7 已知反模式检查
- **反模式**：仅靠服务端限流，客户端无控制 → 连接堆积。
- **规避**：客户端 `actives` 也配置。

---

### 原则 5 推理草稿
- **选择依据**：高可用 → 流控熔断。
- **关联域**：无。
- **预期检查项**：C1,C3,C4,C15。

### 原则 5 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 过载保护 |
| C3 | ✅ | 5.6 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C15 | ✅ | 用户关注“高可用” |

---

## 原则 6：安全与认证（X9）

### 6.1 第一性原理
- **物理/数学约束**：RPC 调用可能被截获、伪造。
- **推导**：认证、授权、加密、防篡改。

### 6.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| Token 认证 | 服务级别静态 Token | 简单 | 安全性低 |
| 服务注册中心认证 | 配合注册中心 ACL | 中等 | 依赖中心 |
| TLS 传输加密 | Dubbo 支持 TLS | 高安全 | CPU 开销 |
| 参数校验 | Validation 过滤器 | 防非法参数 | 额外处理 |

### 6.3 通用实现案例
- **Token**：`<dubbo:provider token="123456" />`，consumer 需携带。
- **TLS**：配置 `dubbo.tls.*` 参数。

### 6.4 生产问题与解决
- **Token 硬编码**：可通过配置中心动态下发。
- **TLS 性能**：使用 `SslProvider.OPENSSL`，启用 session 复用。
- **参数校验失败**：返回 RpcException，需业务处理。

### 6.5 边界条件与风险
- **极值风险**：TLS 握手大量连接 → 增加超时限制。
- **Token 泄漏**：定期更换。

### 6.6 性能量化说明
- TLS 开销：Intel Xeon, AES-GCM-128，约 15% CPU 增加。
- 优化：使用 OPENSSL 提供者，硬件 QAT。

### 6.7 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `token` | 服务 Token | 强随机字符串 | 需重启 | 不携带 token 调用 |
| `dubbo.tls.enable` | 启用 TLS | true | 需重启 | 抓包验证 |
| `validation` | 参数校验 | true | 需重启 | 传入非法参数 |

### 6.7.1 参数联动清单
- **场景**：TLS + Token
- **涉及参数**：两个独立安全机制叠加。
- **风险**：性能叠加。

### 6.8 已知反模式检查
- **反模式**：仅凭 Token 明文传输 → 可被窃取。
- **规避**：Token + TLS 同时使用。

---

### 原则 6 推理草稿
- **选择依据**：安全合规。
- **关联域**：无。
- **预期检查项**：C1,C3,C4,C6,C15。

### 原则 6 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 信息安全 |
| C3 | ✅ | 6.7 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C6 | ✅ | OPENSSL 差异 |
| C15 | ✅ | 用户关注“安全合规” |

---

## 原则 7：可观测性（X10）

### 7.1 第一性原理
- **物理/数学约束**：分布式调用链难以追踪。
- **推导**：指标、日志、链路追踪、QoS 运维。

### 7.2 解决手段与代价
| 手段 | 原理 | 收益 | 代价 |
| :--- | :--- | :--- | :--- |
| 内置 Metrics | Prometheus 导出 | 实时监控 | 存储开销 |
| 链路追踪 | 集成 SkyWalking / Jaeger | 调用链分析 | 采样影响性能 |
| 访问日志 | 记录调用明细 | 问题排查 | 磁盘 IO |
| QoS 命令 | telnet 实时查看 | 运维 | 安全风险 |

### 7.3 通用实现案例
- **Metrics**：`<dubbo:metrics protocol="prometheus" port="20881" />`
- **Trace**：SkyWalking agent 自动埋点。

### 7.4 生产问题与解决
- **指标基数大**：限制 `tag` 维度。
- **QoS 暴露外网**：防火墙限制，或关闭。
- **日志量过大**：采样记录或异步写入。

### 7.5 边界条件与风险
- **极值风险**：访问日志全量打印 → 磁盘爆满。
- **链路追踪采样率低**：遗漏问题链路 → 错误全采样，正常低采样。

### 7.6 健康检查最佳实践
Dubbo 自身暴露 QoS 端口（默认 22222），可通过 `telnet` 执行 `ls`、`ps` 等。K8s 探针建议使用 TCP 端口探测 20880（Provider）或自定义存活接口。

```yaml
livenessProbe:
  tcpSocket:
    port: 20880
  initialDelaySeconds: 60
  periodSeconds: 10
```

### 7.7 参数调优与验证
| 参数 | 核心作用 | 配置原则 | 动态生效 | 验证方法 |
| :--- | :--- | :--- | :--- | :--- |
| `dubbo.metrics.prometheus.port` | Prometheus 端口 | 20881 | 需重启 | `curl /metrics` |
| `dubbo.tracing.enabled` | 开启链路追踪 | true | 需重启 | 查看 trace 数据 |
| `dubbo.qos.enable` | 开启 QoS | true | 在线 | `telnet localhost 22222` |

### 7.7.1 参数联动清单
- **场景**：开启链路追踪
- **涉及参数**：`tracing.sampling-rate`。
- **风险**：高采样率影响性能。

### 7.8 已知反模式检查
- **反模式**：生产开启 QoS 未加防护 → 被恶意 shutdown。
- **规避**：修改 QoS 端口或配置防火墙。

---

### 原则 7 推理草稿
- **选择依据**：运维 → 可观测。
- **关联域**：无。
- **预期检查项**：C1,C3,C4,C7,C15。

### 原则 7 自检证明
| 检查项 | 状态 | 具体引用 |
| :--- | :--- | :--- |
| C1 | ✅ | 分布式调用链难追踪 |
| C3 | ✅ | 7.7 表格动态生效 |
| C4 | ✅ | 无过时推荐 |
| C7 | ✅ | 7.6 TCP 探针 |
| C15 | ✅ | 用户关注“可观测” |

---

## 全局权衡矩阵

| 冲突维度 | 涉及原则 | 权衡分析与架构建议 |
| :--- | :--- | :--- |
| 性能 vs 序列化 | 原则1 | 高吞吐用 Dubbo 协议 + Kryo，跨语言用 Triple |
| 可用性 vs 一致性 | 原则2、4 | 核心交易用 failfast + 注册中心 CP，缓存用 failover + AP |
| 扩展性 vs 复杂性 | 原则3 | 按需扩展，不滥用 SPI |
| 安全 vs 性能 | 原则6 | 内网可降低安全等级，公网必须加密 |
| 可观测 vs 资源 | 原则7 | 错误链路全采样，正常低采样 |

---

## 四纵四横覆盖矩阵

| 维度 | 子维度 | 覆盖原则 | 说明 |
| :--- | :--- | :--- | :--- |
| 横向：资源池化 | 连接管理 | 原则1 | 连接数配置 |
| 横向：通信 | 协议、序列化、路由 | 原则1、5 | Dubbo/Triple、SPI、路由 |
| 横向：数据存储 | 不适用 | — | — |
| 横向：计算 | 集群容错、负载均衡 | 原则2、4 | Failover、负载均衡 |
| 纵向：协同 | 服务发现、注册中心 | 原则4 | ZooKeeper/Nacos |
| 纵向：调度 | 限流、熔断 | 原则5 | executes/actives/Sentinel |
| 纵向：追踪与高可用 | 监控、追踪、日志 | 原则7 | Metrics、Trace、QoS |
| 纵向：部署 | 健康检查 | 原则7 | TCP 探针 |

---

## 关键术语覆盖矩阵

| 术语 | 对应原则 | 说明 |
| :--- | :--- | :--- |
| 性能 | 原则1 | 协议、序列化 |
| 高可用 | 原则2、4 | 容错、注册中心 |
| 扩展性 | 原则3 | SPI |
| 一致性 | — | 无（需外部处理） |
| 安全合规 | 原则6 | Token、TLS |
| 可观测 | 原则7 | Metrics、Trace |
| 成本 | 原则1 | 协议选型影响资源 |
| 可演进性 | 原则3 | SPI 扩展 |
| 长事务 | — | 不适用 |

---

## 待验证参数清单汇总

| 序号 | 参数位置 | 参数描述 | 当前占位符 | 建议验证方法 | 推荐查阅链接 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 原则1.7 | Dubbo 协议 payload | `[需查阅官方文档确认]` | 发送大消息测试 | [Dubbo 配置](https://dubbo.apache.org/zh/docs/) |
| 2 | 原则2.7 | 负载均衡权重 | `[需查阅官方文档确认]` | 压测观察分布 | [Dubbo 负载均衡](https://dubbo.apache.org/zh/docs/advanced/loadbalance/) |
| 3 | 原则7.7 | QoS 端口 | `[需查阅官方文档确认]` | `telnet` 测试 | [Dubbo QoS](https://dubbo.apache.org/zh/docs/advanced/qos/) |
